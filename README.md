# HDOS - Home DOS  
**UNDER CONSTRUCTION!**
EN/RU

EN

**stubs.asm** - stubs for unimplemented functions

**101clock.asm** - real-time clock with blinking colon

**boot.asm** - OS bootloader (first disk sector)

**calc.asm** - calculator with basic operations

**comm.asm** - command dispatcher (handles //commands)

**err.asm** - critical error handler (kernel panic)

**fat32.asm** - FAT32 filesystem stub (not working)

**gui.asm** - graphical user interface (in development)

**hfs.asm** - HDOS File System (in development)

**int.asm** - interrupt handlers (timer)

**kern.asm** - main kernel, entry point after bootloader

**notepad.asm** - text editor

**sh.asm** - command shell

**snake.asm** - Snake game

=======================================================

RU

**stubs.asm** - заглушки для нереализованных функций

**101clock.asm** - часы реального времени с моргающим двоеточием

**boot.asm** - загрузчик ОС (первый сектор диска)

**calc.asm** - калькулятор с базовыми операциями

**comm.asm** - диспетчер команд (обработка //command)

**err.asm** - обработчик критических ошибок (kernel panic)

**fat32.asm** - заглушка FAT32 файловой системы (не работает)

**gui.asm** - графический интерфейс (в разработке)

**hfs.asm** - HDOS File System (в разработке)

**int.asm** - обработчики прерываний (таймер)

**kern.asm** - главное ядро, точка входа после загрузчика

**notepad.asm** - текстовый редактор

**sh.asm** - командная оболочка (shell)

**snake.asm** - игра "Змейка"
